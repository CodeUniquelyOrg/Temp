//
// Written my Steve Saxton <steves@codeuniquely.co.uk>
// Application ui-select default configuration
//

// always ensure that ui-select uses the 'select2' theme for all drop-downs
/* @ngInject */
export default function(uiSelectConfig) {
  // uiSelectConfig.theme = 'select2';
  uiSelectConfig.theme = 'bootstrap';
}